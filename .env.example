# URL Apps Script Web App (для отправки статистики в Google Sheets)
# Создай копию этого файла как .env.local и вставь свою ссылку.
# Файл .env.local не попадает в git и в публичный сайт его нельзя посмотреть в репозитории.
VITE_SHEETS_WEBHOOK=